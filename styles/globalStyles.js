import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  title: {
    fontSize: 24,
    marginBottom: 20,
  },
  containerForm: {
    display: "flex",
    alignItems: "center",
    paddingTop: 120,
    marginHorizontal: 10,
  },
});
